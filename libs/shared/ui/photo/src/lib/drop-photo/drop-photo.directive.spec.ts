import { DropPhotoDirective } from './drop-photo.directive';

describe('DropPhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new DropPhotoDirective();
    expect(directive).toBeTruthy();
  });
});
