import { SocialLoginDirective } from './social-login.directive';

describe('SocialLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new SocialLoginDirective();
    expect(directive).toBeTruthy();
  });
});
